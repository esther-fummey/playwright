import {test} from '../fixtures';

test('Example test with fixtures', async ({ qaURL }) => {
    // Use the qaURL fixture in your test
    //await qaURL.goto(qaURL);


    console.log("My first test")
});
test('get started link', async ({ qaURL }) => {
    // Use the qaURL fixture in your test
    //await qaURL.goto(qaURL);


    console.log("My second test")
});
// test('Example test with fixtures', async ({ qaURL }) => {
//     // Use the qaURL fixture in your test
//     //await qaURL.goto(qaURL);


//     console.log("My first test")
// });
// test('Example test with fixtures', async ({ qaURL }) => {
//     // Use the qaURL fixture in your test
//     //await qaURL.goto(qaURL);


//     console.log("My first test")
// });